import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function TeamPipelinePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Team Pipeline</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Team pipeline view coming soon...</p>
      </CardContent>
    </Card>
  );
}
