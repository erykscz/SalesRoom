import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function SalesRoomCreatePage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Create Sales Room</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Sales room creation wizard coming soon...</p>
      </CardContent>
    </Card>
  );
}
