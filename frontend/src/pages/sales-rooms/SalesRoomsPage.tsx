import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function SalesRoomsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Sales Rooms</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Sales rooms list coming soon...</p>
      </CardContent>
    </Card>
  );
}
