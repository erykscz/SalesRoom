import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function BattlecardsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Battlecards</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Objection battlecard generator coming soon...</p>
      </CardContent>
    </Card>
  );
}
