import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function DealEditPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Edit Deal</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Deal edit form coming soon...</p>
      </CardContent>
    </Card>
  );
}
